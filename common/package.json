{
  "name": "common",
  "version": "1.0.2",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf ./build/*",
    "build": "npm run clean && tsc",
    "pub": "git add . && git commit -m \"Updates\" && npm version patch && npm run build && npm publish"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "rimraf": "^5.0.5"
  }
}
